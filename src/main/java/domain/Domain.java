package domain;

/**
 * 领域
 * 1、核心域
 * 2、支撑子域
 * 3、通用子域
 *
 * 界限上下文
 * 1、界限上下文并不只局限于容纳模型，它通常标定了一个系统、一个应用程序或者一种业务服务
 * 2、界限上下文包括了诸如 模块，聚合，领域事件，领域服务等
 *
 * 上下文映射图
 * 映射图反映的是模块与模块之间的交互，包括外部依赖模块，和内部模块之间的写作交互
 *
 */
public class Domain {
}
